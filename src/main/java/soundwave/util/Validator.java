package soundwave.util;

public class Validator {
    public Validator(Object model, String key, String rule) {
        switch (rule) {
            case "unique":

                break;
            default:
                break;
        }
    }
}
